/**
 * @typedef {Object} FieldOptions
 * @property {Node} node
 * @property {string} channelId
 * @property {string} sdkId
 */

/**
 * @typedef {Object} FieldDataChangePayload
 * @property {boolean} isDirty - is field empty or dirty
 * @property {boolean} isValid - is field valid or invalid after changes
 */

/**
 * @typedef {Object} FieldFocusChangePayload
 * @property {boolean} isFocused - is field focused
 */
