/**
 * @enum {string}
 */
const CONTROLLER_NODE_STATUSES = {
  MOUNTED: 'mounted',
  UNMOUNTED: 'unmounted',
  DESTROYED: 'destroyed',
};
/**
 * @enum {string}
 */
const CONTROLLER_VALIDATION_STATUSES = {
  VALID: 'valid',
  INVALID: 'invalid',
  UNKNOWN: 'unknown',
};
/**
 * @enum {string}
 */
const CONTROLLER_READINESS_STATUSES = {
  LOADING: 'loading',
  READY: 'ready',
  DESTROYED: 'destroyed',
};

const INITIAL_CONTROLLER_STATUS = {
  node: CONTROLLER_NODE_STATUSES.UNMOUNTED,
  readiness: CONTROLLER_READINESS_STATUSES.LOADING,
};

const DESTROYED_CONTROLLER_STATUS = {
  node: CONTROLLER_NODE_STATUSES.DESTROYED,
  validation: CONTROLLER_VALIDATION_STATUSES.UNKNOWN,
  readiness: CONTROLLER_READINESS_STATUSES.DESTROYED,
};

/**
 * Controller statuses
 * @enum {string}
 */
const CONTROLLER_STATUSES = {
  INITIAL: INITIAL_CONTROLLER_STATUS,
  DESTROYED: DESTROYED_CONTROLLER_STATUS,
  NODE: CONTROLLER_NODE_STATUSES,
  VALIDATION: CONTROLLER_VALIDATION_STATUSES,
  READINESS: CONTROLLER_READINESS_STATUSES,
};

export {
  CONTROLLER_NODE_STATUSES,
  CONTROLLER_VALIDATION_STATUSES,
  CONTROLLER_READINESS_STATUSES,
  CONTROLLER_STATUSES,
  INITIAL_CONTROLLER_STATUS,
  DESTROYED_CONTROLLER_STATUS,
};
